import React from "react";
import ReactDOM from "react-dom/client";
import MultiplicationTrainer from "./MultiplicationTrainer";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MultiplicationTrainer />
  </React.StrictMode>
);
